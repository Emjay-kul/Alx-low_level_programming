#include "main.h"
#include <stdio.h>
/**
 * _abs - code to print absolute value 
 *
 * Return: The absolute value
 */
int _abs(int)
{
	int n;

	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}


